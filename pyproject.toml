[project]
name = "pymmary"
dynamic = ["version"]
description = "Agent-optimized output compressor for Python tooling."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Unay Santisteban", email = "usantisteban@othercode.io" }
]
keywords = ["testing", "ai-agents", "pytest", "output", "compression"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Framework :: Pytest",
    "Topic :: Software Development :: Testing",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/othercodes/pymmary"
Repository = "https://github.com/othercodes/pymmary.git"
Issues = "https://github.com/othercodes/pymmary/issues"

[project.optional-dependencies]
# 9.1 is a hard floor, not a preference, and dependabot must not move it.
# See the ignore rule in .github/dependabot.yml.
#   - 8.0 for @pytest.hookimpl(wrapper=True)
#   - 9.1 because the adapter unregisters the terminal reporter, and until 9.1
#     _pytest/assertion/util.py built its explanation through
#     config.get_terminal_writer(), which asserts that reporter still exists.
#     On 8.x and 9.0 every bare-assert failure comes out as a bare
#     "AssertionError" pointing into pytest's own internals.
# The test-min-pytest CI job pins this exact version to keep the claim honest.
pytest = ["pytest>=9.1"]

[project.entry-points.pytest11]
pymmary = "pymmary.adapters.pytest"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"
fallback-version = "0.0.0.dev0"

[tool.hatch.build.hooks.vcs]
version-file = "src/pymmary/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/pymmary"]

[dependency-groups]
test-core = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    # Only to prove the adapter stays a no-op under distributed runs.
    "pytest-xdist>=3.8.0",
]
dev = [
    {include-group = "test-core"},
    "ruff>=0.16.1",
    "mypy>=2.3.0",
]

[tool.ruff]
target-version = "py310"
line-length = 120
indent-width = 4
exclude = [
    ".eggs",
    ".git",
    ".pytest_cache",
    ".ruff_cache",
    ".venv",
    "build",
    "dist",
    "src/pymmary/_version.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
pythonpath = [".", "src"]
addopts = "-p no:pymmary --cov=pymmary --cov-report=term-missing --cov-fail-under=100"

[tool.coverage.run]
branch = true
source = ["src/pymmary"]
omit = ["src/pymmary/_version.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "pragma: no branch",
    "if TYPE_CHECKING:",
]

[tool.mypy]
python_version = "3.10"
exclude = ["src/pymmary/_version\\.py$"]
strict = false
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = false
